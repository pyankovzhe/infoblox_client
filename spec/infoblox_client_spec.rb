require 'spec_helper'

describe InfobloxClient do
  it 'has a version number' do
    expect(InfobloxClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
